# 贡献方式

欢迎推荐论文、纠正发表信息、补充实验设置和更新代码入口。

## 推荐一篇论文

请使用 [论文记录模板](templates/paper.md)，提供原文及 DOI/arXiv 标识、期刊或会议与日期、主要痛点、数据集、方法、结论。说明该工作与 AIDD 的关系，以及支持结论的原文表格或章节。

优先提供一篇证据完整的论文；正式发表、预印本、观点、数据基准分别标注。不要仅引用搜索摘要、新闻宣传或二手榜单证明 SOTA。

## 更新记录

编辑 `data/papers.json` 中的命名字段，然后运行：

进入 README 的核心论文还需填写 `one_liner`，用一句直白的中文说明“研究什么、主要怎么做”。避免仅列关键词或用“达到 SOTA”等评价代替内容概括。

```bash
python scripts/build.py
python scripts/build.py --check
```

单篇笔记、首页、专题索引和 CSV 由脚本生成，不单独手工修改，以免与数据文件不一致。当前生成器支持 ADMET 专题；新增其他专题时，同时扩展专题组织和校验逻辑。

`publication.date` 允许 `YYYY`、`YYYY-MM`、`YYYY-MM-DD` 三种精度，`date_basis` 说明会议年份、期刊在线日期、卷期月份或预印本日期。缺少代码链接时使用 `null`，并在 `code_status` 中说明，不猜测仓库地址。

核验日期应对应实际核验行为。请勿把“找到代码”写成“已复现”，也不要将未核实的结果补成确定数值。

## 报告错误

提供条目名、错误字段、建议更正和原始来源链接。对数值更正，请注明数据版本、划分、指标、表格位置与原文版本。
